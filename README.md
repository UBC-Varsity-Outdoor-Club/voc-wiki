# VOC Wiki
## Instructions for setting up the Wiki on a new machine ##
1. Clone this repository
2. Create a file named `.env`, and add the settings included in `.env.sample`
3. Make a copy of `LocalSettings.php.sample` (remove the `.sample` suffix), and copy the same credentials from `.env` into `$wgDBName, $wgDBuser, $wgDBpassword`